{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "src", // These are relative to your tsconfig.json file
    "outDir": "dist" // These are relative to your tsconfig.json file
  },
  "include": ["src/**/*"],
  "references": [{ "path": "../core" }]
}
